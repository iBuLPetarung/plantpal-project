import 'package:flutter/material.dart';

class StartPlantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Start Plant')),
      body: Center(
        child: Text('Welcome to the Start Plant Page!'),
      ),
    );
  }
}